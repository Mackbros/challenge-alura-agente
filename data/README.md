# data/

Coloca aquí el PDF fuente con nombre **`guia_fiscal_sat.pdf`**.

El agente lo indexa en el primer arranque y guarda el índice FAISS en `data/faiss_index/`
(ignorado por git). Para reindexar tras cambiar el PDF, borra esa carpeta.
